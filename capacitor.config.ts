import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nak3deye.missioncontrol',
  appName: 'Mission Control',
  webDir: 'dist'
};

export default config;
